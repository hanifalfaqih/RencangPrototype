<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RencangPrototype" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RencangPrototype" parent="Base.Theme.RencangPrototype" />

    <style name="App.Custom.BottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="behavior_peekHeight">400dp</item>
        <item name="behavior_fitToContents">false</item>
    </style>
</resources>